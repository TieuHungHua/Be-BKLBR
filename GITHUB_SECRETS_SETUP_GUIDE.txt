================================================================================
                    HƯỚNG DẪN THÊM SECRETS VÀO GITHUB
================================================================================

📌 TỔNG QUAN:
   GitHub Secrets cho phép lưu trữ thông tin nhạy cảm (credentials, API keys)
   một cách an toàn và sử dụng trong CI/CD workflows.

================================================================================
🔍 CÁCH LẤY CÁC SECRETS
================================================================================

1. DATABASE_URL
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📍 Nguồn: Neon Database (hoặc database khác bạn đang dùng)

Cách lấy:
   1. Vào Neon Dashboard: https://console.neon.tech
   2. Chọn project của bạn
   3. Vào tab "Connection Details" hoặc "Settings"
   4. Copy "Connection String" hoặc "Connection URI"
   5. Format: postgresql://user:password@host:port/database?sslmode=require

Ví dụ (của bạn):
   postgresql://neondb_owner:npg_sSDtRa5Jxi0b@ep-delicate-mouse-a40czuxz-pooler.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

⚠️  LƯU Ý: 
   - Nên tạo database riêng cho production (không dùng chung với dev)
   - Connection string production có thể khác với dev

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

2. JWT_SECRET
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📍 Nguồn: File .env trong backend/

Cách lấy:
   1. Mở file backend/.env
   2. Tìm dòng JWT_SECRET=...
   3. Copy giá trị sau dấu =

Hoặc tạo mới:
   - Sử dụng random string generator
   - Tối thiểu 32 ký tự
   - Nên có chữ hoa, chữ thường, số, ký tự đặc biệt

Ví dụ:
   your-super-secret-jwt-key-change-in-production-min-32-chars

⚠️  LƯU Ý:
   - Production nên dùng secret mạnh hơn
   - Không share secret này với ai
   - Có thể generate bằng: openssl rand -base64 32

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

3. CLOUDINARY_CLOUD_NAME
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📍 Nguồn: Cloudinary Dashboard

Cách lấy:
   1. Vào Cloudinary Dashboard: https://console.cloudinary.com
   2. Đăng nhập vào tài khoản
   3. Vào "Settings" (biểu tượng bánh răng) hoặc "Dashboard"
   4. Tìm "Account Details" hoặc "Product Environment Credentials"
   5. Copy "Cloud name"

Ví dụ (của bạn):
   dy7potpml

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

4. CLOUDINARY_API_KEY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📍 Nguồn: Cloudinary Dashboard

Cách lấy:
   1. Vào Cloudinary Dashboard: https://console.cloudinary.com
   2. Vào "Settings" > "Security" hoặc "Product Environment Credentials"
   3. Tìm "API Key"
   4. Copy giá trị

Ví dụ (của bạn):
   661388175111363

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

5. CLOUDINARY_API_SECRET
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📍 Nguồn: Cloudinary Dashboard

Cách lấy:
   1. Vào Cloudinary Dashboard: https://console.cloudinary.com
   2. Vào "Settings" > "Security" hoặc "Product Environment Credentials"
   3. Tìm "API Secret"
   4. Click "Reveal" để hiện giá trị (nếu bị ẩn)
   5. Copy giá trị

Ví dụ (của bạn):
   ER71CSmE8PRx7VOJ3Sc4ufq8Aq8

⚠️  LƯU Ý:
   - API Secret rất nhạy cảm, không share với ai
   - Nếu quên, có thể reset trong Cloudinary Dashboard

================================================================================
📝 CÁCH THÊM VÀO GITHUB SECRETS
================================================================================

BƯỚC 1: Vào GitHub Repository
   1. Mở repository trên GitHub
   2. Click vào tab "Settings" (ở trên cùng)

BƯỚC 2: Vào Secrets and variables
   1. Trong menu bên trái, tìm "Secrets and variables"
   2. Click vào "Actions"

BƯỚC 3: Thêm từng Secret
   1. Click nút "New repository secret"
   2. Điền:
      - Name: DATABASE_URL (chính xác như vậy)
      - Secret: Dán connection string
   3. Click "Add secret"
   4. Lặp lại cho các secrets còn lại:
      - JWT_SECRET
      - CLOUDINARY_CLOUD_NAME
      - CLOUDINARY_API_KEY
      - CLOUDINARY_API_SECRET

BƯỚC 4: Kiểm tra
   Sau khi thêm xong, bạn sẽ thấy danh sách secrets (chỉ hiện tên, không hiện giá trị)

================================================================================
📋 CHECKLIST
================================================================================

☐ Đã lấy DATABASE_URL từ Neon Dashboard
☐ Đã lấy JWT_SECRET từ .env hoặc tạo mới
☐ Đã lấy CLOUDINARY_CLOUD_NAME từ Cloudinary
☐ Đã lấy CLOUDINARY_API_KEY từ Cloudinary
☐ Đã lấy CLOUDINARY_API_SECRET từ Cloudinary
☐ Đã thêm tất cả vào GitHub Secrets
☐ Đã test workflow chạy thành công

================================================================================
🔐 SECURITY TIPS
================================================================================

1. KHÔNG commit secrets vào code
   - Đã có trong .gitignore
   - Chỉ dùng GitHub Secrets

2. Sử dụng secrets khác nhau cho dev và production
   - Dev: Có thể dùng trong .env local
   - Production: Chỉ dùng GitHub Secrets

3. Rotate secrets định kỳ
   - Đổi JWT_SECRET mỗi 3-6 tháng
   - Đổi Cloudinary API Secret nếu bị lộ

4. Kiểm tra quyền truy cập
   - Chỉ admin/owner mới thấy được secrets
   - Review ai có quyền vào repository

================================================================================
🧪 TEST SECRETS
================================================================================

Sau khi thêm secrets, test bằng cách:

1. Push code lên GitHub
2. Vào tab "Actions" trong repository
3. Xem workflow run
4. Kiểm tra logs xem có lỗi về missing secrets không

Nếu workflow chạy thành công → Secrets đã được cấu hình đúng ✅

================================================================================
📞 TROUBLESHOOTING
================================================================================

Lỗi: "Secret not found"
   → Kiểm tra tên secret có đúng không (case-sensitive)
   → Đảm bảo đã thêm vào repository secrets (không phải environment secrets)

Lỗi: "Invalid DATABASE_URL"
   → Kiểm tra connection string có đúng format không
   → Đảm bảo database đã được wake up (nếu dùng Neon)

Lỗi: "Cloudinary authentication failed"
   → Kiểm tra API Key và API Secret có đúng không
   → Đảm bảo không có khoảng trắng thừa khi copy

================================================================================
                    END OF GUIDE
================================================================================

